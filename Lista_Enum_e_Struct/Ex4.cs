using System;

namespace C_.Lista_Enum_e_Struct
{
    public class Ex4
    {
        struct Ponto
        {
            public int x, y;
        }

        /*
        static void Main(String[] args)
        {
            Ponto p1, p2;
            Console.Write("Informe o valor de x do ponto 1: ");
            p1.x = int.Parse(Console.ReadLine());
            Console.Write("Informe o valor de y do ponto 1: ");
            p1.y = int.Parse(Console.ReadLine());

            Console.Write("Informe o valor de x do ponto 2: ");
            p2.x = int.Parse(Console.ReadLine());
            Console.Write("Informe o valor de y do ponto 2: ");
            p2.y = int.Parse(Console.ReadLine());
         
            Console.WriteLine("A distância entre os pontos é {0}", Math.Sqrt(Math.Pow(p2.x - p1.x, 2) + Math.Pow(p2.y - p1.y, 2)));
        }
        */
    }
}