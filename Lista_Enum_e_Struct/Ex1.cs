using System;

namespace C_.Lista_Enum_e_Struct
{
    public class Ex1
    {
        enum DiasSemana { Domingo = 1, Segunda, Terça, Quarta, Quinta, Sexta, Sábado};

        /*
        static void Main(String[] args)
        {
            for (int i = 1; i <= DiasSemana.GetValues(typeof(DiasSemana)).Length; i++)
            {
                Console.WriteLine("{0} é o {1}º dia da semana", (DiasSemana)i, i);
            }
        }
        */
    }
}