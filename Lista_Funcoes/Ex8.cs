using System;

namespace C_.Lista_Funcoes
{
    public class EX8
    {
        /*
        static void Main(String[] args)
        {
            calcula();
        }

        static void calcula()
        {
            Console.Write("Digite um número inteiro: ");
            int N = int.Parse(Console.ReadLine());
            double num = 0, denom = 0, S = 0;

            for(int i = 1; i <= N; i++)
            {
                num = Math.Pow(i, 2) + 1;
                denom = i + 3;
                S += num / denom;
            }
            Console.WriteLine("S = " + S);
        }
        */
    }
}